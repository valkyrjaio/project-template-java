# The template ships with an empty source tree (users replace src/ with their
# own package). Allow architecture rules that match no classes to pass rather
# than fail, so CI is green on a fresh, un-customized template.
archRule.failOnEmptyShould=false
